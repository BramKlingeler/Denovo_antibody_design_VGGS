#!/usr/bin/env python

from setuptools import find_packages, setup

setup(
    name="ggs",
    version="0.0.2",
    description="This project implements Varying length Gibbs sampling with Graph-based Smoothing (GGS) on SARS-CoV-2 data ",
    packages=find_packages(),
)
